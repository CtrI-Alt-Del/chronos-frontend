import { Accordion, type AccordionProps } from '@heroui/accordion'

export const SolicitationsAccordionContainerView = (props: AccordionProps) => {
  return <Accordion {...props} className='border border-gray-border rounded-lg px-4' />
}
